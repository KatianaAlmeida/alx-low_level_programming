#include <stdio.h>

/**
 * main - Numbers
 *
 * Return: Always 0 (Sucess)
 */

int main(void)
{
	int index;
	for (index =0; index <10; index++)
	{
		printf("%d", index);
	}
	putchar('\n');
	return (0);
}
