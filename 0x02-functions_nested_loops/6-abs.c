#include "main.h"

/**
 * _abs - calculates the absolute value of a number
 * @n: hold the number to check
 * Return: the absolute value of an integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
